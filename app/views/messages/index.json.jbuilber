json.array! @new_messages do |new_message|
  json.body      new_message.content
  json.id        new_message.id
  json.image     new_message.image
  json.user_name new_message.user.name
  json.date      new_message.created_at
